/**
 * @typedef BackButtonProps
 *
 * @property {string} text - Text to display on the button.
 * @property {string} link - Link to navigate to.
 */
export interface BackButtonProps {
  text: string;
  link: string;
}
